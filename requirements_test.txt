# Everything the suite imports must be declared here so a fresh clone can run
# `pip install -r requirements_test.txt && pytest tests/` (the README's
# Contributing instructions). CI additionally pins the homeassistant version
# per matrix leg on the pip command line — the unpinned entry below is the
# local default and is overridden by those pins.
homeassistant
aiohttp
PyYAML
# Pinned to what pytest-homeassistant-custom-component requires (it pins
# pytest exactly). Holding a newer pytest here makes the current-HA leg
# unresolvable, and the two must agree for the fixture-backed flow tests to
# install at all. pytest-asyncio and pytest-cov already match its pins.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# Static analysis, run by the `lint` job in tests.yaml. Pinned like the rest so
# a new ruff release cannot turn a green branch red without a deliberate bump.
ruff==0.16.2
mypy==2.3.0

# NOT listed here on purpose: pytest-homeassistant-custom-component. It pins one
# exact Home Assistant version per release, so putting it in this file would
# force that version on every CI leg and break the minimum-HA lane. The
# workflow installs it only on the leg whose HA version it matches; the
# fixture-backed tests in tests/test_config_flow_hass.py skip where it is absent.
